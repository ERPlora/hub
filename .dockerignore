# ==============================================================================
# ERPlora Hub - Docker Ignore
# ==============================================================================
# Files and directories to exclude from Docker build context
# This speeds up build and reduces image size
# ==============================================================================

# Python bytecode and cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments (use system Python in Docker)
.venv/
venv/
ENV/
env/

# Django local files (will be recreated in container)
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Test and coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
tests/
conftest.py
pytest.ini

# IDE and editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Git (not needed in image)
.git/
.gitignore
.gitattributes

# Documentation (not needed in runtime, except docs/README.md for pyproject.toml)
docs/*
!docs/README.md
CLAUDE.md
DOCKER.md
README.md
CHANGELOG.md
LICENSE

# CI/CD
.github/
.gitlab-ci.yml

# PyInstaller (desktop build, not for Docker)
*.spec
main.py
build.py
kiosk/
installers/
assets/

# Development environment files
.env
.env.local
.env.example
.env.*.local

# Node (if present)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Backup and temp files
*.bak
*.backup
*~
backups/

# Lock files (not needed, we install from pyproject.toml)
uv.lock

# Modules in development (underscore prefix = disabled)
modules/_*
modules/.template/

# Logs (will be generated in volume)
logs/
*.log

# Scripts (development only)
scripts/

# Cache directories
cache/
.cache/
