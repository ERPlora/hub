# ERPlora Hub - Environment Variables
# Copy to .env and customize (optional — Hub works without .env)

# =============================================================================
# ENVIRONMENT
# =============================================================================
# HUB_ENV selects the settings module:
#   local       — Development on your machine (default if empty)
#   sandbox     — Isolated paths (ERPloraHub-sandbox/) — wipes on each startup
#   web         — Docker/Cloud deployment via Dokploy

# HUB_ENV=local

# =============================================================================
# OVERRIDES (all optional — sensible defaults auto-detected)
# =============================================================================

# Cloud API URL (default: https://erplora.com)
# CLOUD_API_URL=https://erplora.com

# Secret key (auto-generated and persisted to .secret_key if empty)
# SECRET_KEY=

# Development mode (default: true — relaxes module signature checks)
# ERPLORA_DEV_MODE=true

# Override database path (default: auto-detected per platform)
# DATABASE_PATH=/path/to/db.sqlite3

# Override modules directory (default: auto-detected per platform)
# MODULES_DIR=/path/to/modules

# UX library dist path (default: ../../ux/dist relative to hub/)
# UX_DIST_PATH=/path/to/ux/dist

# Django debug toolbar
# DEBUG_TOOLBAR=true

# Hub local port (default: 8001)
# HUB_LOCAL_PORT=8001

# =============================================================================
# WEB/DOCKER ONLY (injected by Dokploy — not needed for local dev)
# =============================================================================
# DEPLOYMENT_MODE=web
# HUB_ID=uuid
# HUB_NAME=tienda-de-maria
# PARENT_DOMAIN=erplora.com
# SERVER_DOMAIN=a.erplora.com
# HUB_JWT=...
# DATABASE_URL=postgres://...
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
