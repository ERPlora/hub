# ERPlora Hub - Environment Variables
# Copy to .env and customize
# SECRET_KEY se genera automáticamente si no existe

# =============================================================================
# DEPLOYMENT MODE
# =============================================================================
# local (default)  — Development on your machine
# web              — Docker/Cloud deployment via Dokploy

# DEPLOYMENT_MODE=local

# =============================================================================
# HUB IDENTITY
# =============================================================================
# HUB_ID defaults to "local" for development.
# In Docker/Cloud, injected by Dokploy.

# HUB_ID=local
# HUB_NAME=hub

# =============================================================================
# DATA PATHS
# =============================================================================
# Same directory structure in local and Docker: {VOLUME_PATH}/hubs/{HUB_ID}/
#
# Local default:  ~/.erplora/hubs/local/
# Docker default: /data/hubs/{uuid}/
#
# Structure:
#   ~/.erplora/hubs/local/db/db.sqlite3  — SQLite database
#   ~/.erplora/hubs/local/modules/        — Installed modules
#   ~/.erplora/hubs/local/media/          — Media files
#   ~/.erplora/hubs/local/backups/        — Backups
#   ~/.erplora/hubs/local/logs/           — Log files
#   ~/.erplora/hubs/local/reports/        — Generated reports

# VOLUME_PATH=~/.erplora                 # local default (auto)
# VOLUME_PATH=/data                      # Docker default (auto)
# DATABASE_URL=                          # PostgreSQL URL (overrides SQLite)
# MODULES_DIR=                           # Override modules path

# =============================================================================
# WEB/DOCKER (injected by Dokploy)
# =============================================================================
# DEPLOYMENT_MODE=web
# HUB_ID=a1b2c3d4
# HUB_NAME=tienda-de-maria
# DATABASE_PATH=/data/hubs/a1b2c3d4/db/db.sqlite3
# PARENT_DOMAIN=erplora.com
# SERVER_DOMAIN=a.erplora.com
# CLOUD_BASE_URL=https://erplora.com
# HUB_JWT=...
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_STORAGE_BUCKET_NAME=...
# AWS_S3_ENDPOINT_URL=...
# AWS_S3_REGION_NAME=...
# AWS_LOCATION=...

# =============================================================================
# OPTIONAL
# =============================================================================
# DEBUG=true
# DEBUG_TOOLBAR=true
# CLOUD_API_URL=https://erplora.com
