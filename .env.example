# CPOS Hub - Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Cloud API
# URL of the Cloud server for authentication and hub registration
CLOUD_API_URL=http://localhost:8000

# Hub local server port (where Django runs)
HUB_LOCAL_PORT=8001

# Database (SQLite - no configuration needed)
# DB_PATH is auto-configured to use user data directory

# Logging
LOG_LEVEL=INFO

# Deployment Mode
# Controls print behavior and other environment-specific features
# Options: 'local' (desktop app with PyWebView) or 'web' (browser-based)
# Default: 'local'
DEPLOYMENT_MODE=local

# Local Print Service URL (for web deployments with local printer access)
# Only used when DEPLOYMENT_MODE=web and local print service is available
# LOCAL_PRINT_SERVICE_URL=http://localhost:8080

# NOTE: FRP (Fast Reverse Proxy) configuration is handled by the multi_device plugin
# If you need remote access to this Hub, install and configure the multi_device plugin
